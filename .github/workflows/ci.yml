# Generated by haskell-ci-orchestrator — DO NOT EDIT MANUALLY
name: browserless-tooling CI
on:
  push:
    branches: [main]
    tags:
      - v*.*.*
      - v*
  pull_request:
  workflow_dispatch:
  schedule:
    - cron: '0 4 * * 1'
permissions:
  contents: write
  id-token: write
  attestations: write
  security-events: write
concurrency:
  group: browserless-tooling-ci-${{ github.ref }}
  cancel-in-progress: true
jobs:
  lint:
    name: Lint
    runs-on: [self-hosted, unified-all]
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Bash syntax check
        shell: bash
        timeout-minutes: 5
        run: find . -name '*.sh' -type f | xargs -I{} bash -n {}
      - name: ShellCheck
        shell: bash
        continue-on-error: true
        timeout-minutes: 5
        run: find . -name '*.sh' -type f | xargs shellcheck --severity=warning
      - name: Shell formatting (shfmt)
        shell: bash
        continue-on-error: true
        timeout-minutes: 5
        run: find . -name '*.sh' -type f | xargs shfmt -d
  test:
    name: Test
    runs-on: [self-hosted, unified-all]
    needs: lint
    timeout-minutes: 30
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Cache /tmp/docker-runner
        uses: actions/cache@v5
        with:
          key: ${{ runner.os }}-docker-runner-${{ github.sha }}
          path: /tmp/docker-runner
          restore-keys: ${{ runner.os }}-docker-runner-

      - name: Bash syntax
        shell: bash
        timeout-minutes: 5
        run: find . -name '*.sh' -type f | xargs -I{} bash -n {}
  security:
    name: Security
    runs-on: [self-hosted, unified-all]
    needs: lint
    timeout-minutes: 30
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Secrets scan (gitleaks)
        shell: bash
        timeout-minutes: 10
        run: gitleaks detect --source=. --no-banner --exit-code=1
      - name: ShellCheck
        shell: bash
        continue-on-error: true
        timeout-minutes: 10
        run: find . -name '*.sh' -type f | xargs shellcheck --severity=warning
      - name: Shell formatting (shfmt)
        shell: bash
        continue-on-error: true
        timeout-minutes: 5
        run: find . -name '*.sh' -type f | xargs shfmt -d
  sbom:
    name: SBOM
    runs-on: [self-hosted, unified-all]
    needs: test
    if: github.ref == 'refs/heads/main'
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Generate SBOM (hand-crafted CycloneDX)
        shell: bash
        timeout-minutes: 5
        run: echo 'Hand-crafted SBOM — see workflow for inline CycloneDX JSON'
